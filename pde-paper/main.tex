\documentclass[11pt]{article}

\input{preamble}

\title{Cancellation before absolute values in branching representations with derivative weights}
\author{Maciej G{\l}uchowski}
\date{}

\begin{document}

\maketitle

\begin{abstract}
Branching representations for nonlinear parabolic equations are often exact before absolute values are taken and nonintegrable afterward. We study this distinction for representations with derivative weights. First, for the coding-tree construction of Nguwi, Penent, and Privault, we isolate a repeated-Hessian genealogy which forces failure of $L^1$ whenever an even terminal jet grows faster than the ordered-simplex scale. Conversely, integrability of the relevant code-rooted trees imposes a Gevrey-$1/2$ bound on the directional jet. A benchmark equation then shows that this failure is representation-dependent: the coding-tree functional is non-$L^1$ at every positive horizon while a marked branching construction of Henry-Labord\`ere, Oudjane, Tan, Touzi, and Warin is $L^2$ for short time.

The main results concern
\begin{equation*}
  \partial_t v=\frac12v_{xx}+\lambda(v_{xx})^2
\end{equation*}
on the one-dimensional torus. Finite Duhamel trees admit an exact regrouping into maximal Hessian patches. Averaging the continuous variables inside each decorated skeleton before taking absolute values yields, under an explicit small-data condition, an absolutely convergent skeleton expansion and an unbiased $L^1$ estimator. In contrast, for one fixed arbitrarily small smooth datum, every raw-faithful estimator has infinite first moment. Raw-faithfulness requires the canonical raw signed marked integrand to remain the conditional barycenter after the raw marks are exposed; the obstruction is invariant under lifetime, genealogy, and Gaussian proposal changes.

For each fixed observation target, we characterize the entire skeleton-preserving coarsening class. If the raw signed measure of skeleton $\tau$ is $\mu_\tau=R_\tau\nu_\tau$ and $\mathcal C_\tau$ is a coarsening, then its exact first-moment cost is
\begin{equation*}
  \| (\mathcal C_\tau)_\#\mu_\tau\|_{\mathrm{TV}}
  =
  \int
  \left|
    \mathbb E_{\nu_\tau}
    [R_\tau\mid\sigma(\mathcal C_\tau)]
  \right|d\nu_\tau.
\end{equation*}
Summability of these residual signed variations is necessary and sufficient for $L^1$ in the fixed-target coarsened conditional-barycenter class. This pointwise characterization does not itself produce one coarsening architecture that works simultaneously for all targets. At a fixed target one may retain the entire Gaussian configuration on sufficiently small pieces and remain integrable; separately, an abstract signed-measure example shows that retaining only a time coordinate can fail. The raw-faithful and fully averaged constructions are the two structured endpoints, while a time-spine construction gives one target-uniform intermediate architecture under an additional smallness condition.
\end{abstract}

\input{sections/introduction}
\input{sections/preliminaries}
\input{sections/coding-tree-obstructions}
\input{sections/representation-dichotomy}
\input{sections/quadratic-hessian-patches}
\input{sections/skeleton-averaged-representation}
\input{sections/raw-faithful-obstruction}
\input{sections/coarsening-hierarchy}

\printbibliography

\end{document}
