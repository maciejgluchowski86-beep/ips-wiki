# Project state

This file is the compact current-state index for the autonomous research programme. `CHATGPT.md` governs workflow; detailed mathematics lives under `research/active/` and in Git history.

## Current research status

There is presently **no credible active proof architecture** in either of the two recently tested targets.

- Branch: `research/ergodicity-methods-toolbox`.
- Workspace: `research/active/ergodicity-methods-toolbox/`.
- Latest meeting: `meetings/025-fa-info-no-iterable-state-and-fa-quench-stop.md`, `state_narrowed: yes`.
- Current live proof spine: `research/active/ergodicity-methods-toolbox/proof-spine.md`.
- Professor is the only operational executing session at this record. Student G is unavailable for input; Student F's old assignment remains unexecuted/unavailable.

The proof spine is now a live problem-specific document. It is **not** the frozen 74-method coverage map.

## Frozen toolbox status

The ergodicity-method inventory remains frozen at **74 source-audited live methods**. Collection, both full applicability audits, both hostile cross-reviews, and final synthesis remain complete.

The frozen inventory should be checked against:

- the 74 live method pages;
- assessment files and final priorities;
- earlier toolbox meeting history;
- `validate_entries.py` and the public-doc structural gates.

Do not require the current live `proof-spine.md` to mirror the 74-method inventory.

No research block here edits `docs/` or `mkdocs.yml`.

## Positive-rates direction: no credible route

The direct branch `research/positive-rates-conjecture` ended its bounded toolbox-derived tests in Meeting 034.

Stopped direct forms:

1. uniform negative additive-Hamming drift: impossible at the hard point for every Markovian coupling;
2. mark-only deterministic-Boolean information percolation: decomposition-independent pair-intersection obstruction;
3. Gray scalar/two-type splice geometry: ordinary and checkerboard scalar hybrid closure both locally obstructed;
4. the principal-directed zero-boundary-invariant distinguished-zero transfer: reduces to the old tail-shift defect off the product surface.

Meeting 030's signed boundary-transmission restart bar stands. The positive-rates conjecture remains open, but no proof architecture is active for the present group.

## FA-1f Bernoulli-quench direction: no credible route

Scientific target:

> For one-dimensional hard FA-1f with equilibrium vacancy density `q in (0,1)`, prove local convergence from every nondegenerate homogeneous Bernoulli initial law `mu_{q0}`, `q0>0`, to `mu_q`.

The hostile toolbox review retained two independent A/B architectures. Both have now failed their pre-registered bounded gates.

### FA-SCREEN-001

Status: **`STOP-SCALING-OBSTRUCTION`**.

A literal East distinguished vacancy leaks protected information in one ring. The decisive width-three theorem gives every authorized active exterior-measurable vacancy/dimer endpoint a uniform four-unit leakage/failure hazard

$$
\delta_3(q)=e^{-12}q^2(1-q)^2>0.
$$

Hence endpoint age is exponentially tailed. Even `O(s_t)` possible endpoints cannot supply the `s_t -> infinity` fixed fresh interval required by the registered final-volume relaxation stage.

Decisive theorem/verifier:

- `students/professor/001c-fa-screen-width3-scaling-obstruction.md`, commit `8076b527`;
- `001b2-fa-screen-width3-hazard-verifier.py`, commit `71bef127`;
- Meeting 024.

This is not a theorem against every conceivable moving/adaptive boundary screen. It stops the registered fixed-final-interval vacancy-boundary implementation.

### FA-INFO-002

Status: **`STOP-NO-ITERABLE-STATE`**.

The stop rule was frozen before mathematics at commit `ef3dfcfe`.

Adaptive pruning is genuine. At `q=1/10`, a fixed-coin one-ring FA map has three globally essential predecessors, while the optimal exact adaptive evaluator uses only

$$
\frac{671}{500}
$$

queries on average.

The exact adaptive transcript likelihood is

$$
L(Q)=
\prod_j
\left(\frac{q_0}{q}\right)^{1-b_j}
\left(\frac{1-q_0}{1-q}\right)^{b_j},
$$

for predictable fresh-coordinate queries. Value-dependent query-set membership does not break this likelihood identity, but it prevents reducing the transcript to a bare random-set overlap formula.

The bounded pair test separates four states:

1. **raw transcript likelihood:** exact/compositional but noncontractive already on S1 at both registered quenches;
2. **same-graphical-history pair state:** exact/block-local but increases under S1 -> S2, and is already above the one-bit baseline at `q_0=1/20`;
3. **full-predecessor chi-square channel state:** local channel coefficients are below one, but predecessor-vector divergence makes the bound expansive;
4. **fully averaged terminal-output chi-square:** genuinely contracts from product input, but is not closed and increases from S1 to S2.

The exact structural closure theorem is:

for a right-to-left staircase of `m` adjacent equilibrium-coin rings with conditional mean vacancies

$$
M_m=V_m,
$$

$$
M_k=q+(V_k-q)(1-V_{k-1})(1-M_{k+1}),
$$

the coefficient of

$$
\prod_{j=-1}^{m}(V_j-q)
$$

in `M_0-q` is

$$
\boxed{(-q)^{m-1}\ne0.}
$$

Thus exact adjacent composition creates new correlation order at every step. At S2, two positive four-site laws can agree on every proper marginal and still give different terminal output.

The only exact repair currently visible is an enlarging transcript/correlation hierarchy, which the pre-registered assignment forbids.

Decisive files:

- `students/professor/002a-fa-info-adaptive-likelihood.md`, commit `ae910cd9`;
- `002b-fa-info-finite-circuit-closure.md`, commit `c5113b6e`;
- `002c-fa-info-shared-mark-pair.md`, commit `4d76c8c0`;
- final verifier `002-fa-info-finite-circuit-verifier.py`, commit `5bcf597c`;
- final report `002-fa-info.md`, commit `e63191d7`;
- handoff `002-fa-info-handoff.md`, commit `db0ecdc7`;
- Meeting 025.

This is not an impossibility theorem against every imaginable adaptive-information proof. It closes the concrete bounded A/B implementation that survived hostile review.

## Standing stop rules

Do not automatically restart by:

- enlarging FA-SCREEN marker width/phases;
- running a third/fourth FA-INFO block after the S1/S2 closure failure;
- carrying the full growing adaptive transcript/correlation hierarchy;
- replacing FA memory erasure by stronger equilibrium gap/entropy work;
- returning to mark-only information percolation or the conservative centered dual;
- enlarging Gray edge-state hierarchies or generic positive-rates couplings/norms;
- reopening positive-rates tail shift, common-coupling occupation, Bellman/Foster, reversible/filter, or longer coefficient searches without new input.

## Next action

There is no automatic next proof assignment. When research resumes, the Professor should perform a **target-selection/opportunity-cost review** rather than issue a larger instance of a stopped architecture.

A restart in either current problem requires a materially new upstream theorem or architecture with its own bounded falsification test.

## Public-wiki hygiene

The public wiki remains untouched by these research blocks. The toolbox remains at 74 source-audited methods; no `docs/` or `mkdocs.yml` edit is authorized or implied by the current negative research results.
