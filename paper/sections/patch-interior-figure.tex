Figure~\ref{fig:patch-interiors} summarizes the local active histories allowed by
the consistency event for the four finite boundary types.

\begin{figure}[p]
\centering
\input{paper/figures/patch-interiors}
\caption{Finite patch types and their consistent local histories. In each row,
the striped prism on the left records only the patch geometry and its boundary
interactions; the unstriped prisms on the right show the possible local active
histories under the consistent patch law. Gray volume means that the patch site
is active. The drawing uses a square-lattice neighbourhood to visualize
incoming and outgoing interactions. Thus~$\mathsf{IO}$ and~$\mathsf{OO}$ patches
remain active throughout; an~$\mathsf{II}$ patch either remains active or is
killed by a death; and an~$\mathsf{OI}$ patch begins with a hidden split or
birth, with the latter followed either by no death or by a death.}
\label{fig:patch-interiors}
\end{figure}
