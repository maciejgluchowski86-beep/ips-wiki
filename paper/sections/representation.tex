\subsection{Conditional factorization and representation}
\label{sec:representation}

% Previous version preserved in
% paper/revision-reference/section-4-4-conditional-factorization-and-representation.tex.
%
% The patch-interior figure source is unchanged. In the rewrite, the figure
% should appear immediately after the consistent one-patch law has been defined.
%
% The detailed Mecke/Radon--Nikodym proof of patch factorization should be moved
% out of the main text when Blurb D is written. The main text should retain the
% mathematical mechanism and a short proof; the full measure-theoretic
% verification belongs in an appendix.

% ============================================================================
% BLURB A. Why conditioning on the skeleton helps
%
% Reader question:
%   We have fixed the successful-interaction skeleton. What information is still
%   unresolved, and what does its locality buy us?
%
% Job:
%   Say first what G_T fixes and what it leaves unresolved. It fixes all
%   successful interactions and hence the patch decomposition up to time T. It
%   does not reveal the local information deliberately omitted from the
%   skeleton. In particular, deaths inside patches are not recorded, and for a
%   recorded outgoing interaction the skeleton does not say whether it was a
%   split or a birth.
%
%   The fixed patch boundaries restrict this remaining randomness. Depending on
%   the boundary types, they may force an interaction kind or exclude a death.
%   These restrictions involve only one patch at a time. State the payoff and
%   stop there: conditional on G_T, the unresolved randomness factorizes over
%   patches.
%
% Keep out:
%   Sigma_P, alpha(P), Con(P), averaging, cancellations, and the formal
%   factorization theorem. Blurbs B--D make the local law and factorization
%   precise; Blurbs E--F explain what is averaged and why.
% ============================================================================

Conditioning on~$\mathcal G_T$ fixes the successful interactions and hence the
patch decomposition up to time~$T$. It leaves unresolved the local information
that was omitted from the skeleton. In particular, deaths inside patches are
not recorded, and for a recorded outgoing interaction the skeleton does not say
whether it was a split or a birth. The fixed patch boundaries restrict this
remaining randomness: depending on the boundary types, they may force the
interaction kind or exclude deaths. These restrictions involve only one patch
at a time. Consequently, conditional on~$\mathcal G_T$, the unresolved
randomness factorizes over the patches.

% ============================================================================
% BLURB B. Reference law on one patch
%
% Reader question:
%   Before imposing the boundary constraints, what is the local law of the
%   unresolved marks on one patch, and how do those marks determine the
%   one-site process X^P?
%
% Job:
%   Fix a patch P. Define a reference law for the information on its site that is
%   not contained in the skeleton. Inside the patch, this consists of the
%   outgoing Poisson marks on the patch site. If the initial boundary is
%   outgoing, also restore the split/birth kind omitted from that boundary
%   record. The reference law is simply the restriction of the original
%   graphical construction to this local information.
%
%   Define Sigma_P^circ, Sigma_P, and the reference law P_P and expectation E_P.
%   Then define X^P directly under this reference law as the one-site active
%   process generated by the local marks. State its initial value and update
%   rules. Compatibility with the fixed patch boundaries is imposed only in
%   Blurb C.
%
% Formatting:
%   Do not introduce an isolated display merely to abbreviate i(P), s(P), e(P),
%   and S(P). Keep notation economical.
% ============================================================================

Fix a patch~$P$, and write~$i=i(P)$,~$s=s(P)$,~$e=e(P)$, and~$S=S(P)$.
We first define a reference law for the information on the site~$i$ that is not
contained in the skeleton. Inside the patch, the outgoing marks from~$i$ are
\begin{equation*}
\Sigma_P^\circ
=
\cb{(i,u,\gamma,R)\in I:s<u<e}.
\end{equation*}
For each~$R$, the~$(\delta,R)$ and~$(\beta,R)$ marks in~$(s,e)$ are independent
Poisson processes with rates~$\delta_i(R)$ and~$\beta_i(R)$, respectively.

It remains to specify the information at the initial boundary. If
$\mathsf X(P)=\mathsf I$, the boundary is incoming, so there is no outgoing
mark from~$i$ to add and we set~$\Sigma_P=\Sigma_P^\circ$. If
$\mathsf X(P)=\mathsf O$, the boundary record specifies the target~$S$ but not
whether the interaction is a split or a birth. Let
$\alpha(P)\in\cb{\delta,\beta}$ denote its kind, independent of
$\Sigma_P^\circ$, with
\begin{equation}
\label{eq:patch-initial-kind-law}
\pP_P\bb{\alpha(P)=\delta}
=
\frac{\delta_i(S)}{\delta_i(S)+\beta_i(S)},
\qquad
\pP_P\bb{\alpha(P)=\beta}
=
\frac{\beta_i(S)}{\delta_i(S)+\beta_i(S)}.
\end{equation}
In this case,
\begin{equation*}
\Sigma_P
=
\Sigma_P^\circ\cup\cb{(i,s,\alpha(P),S)}.
\end{equation*}
Together, the interior Poisson marks and, when present, the random initial kind
define the reference patch law~$\pP_P$. Write~$\E_P$ for expectation under
$\pP_P$.

The marks in~$\Sigma_P$ determine a one-site process~$X^P\in\cb{0,1}$, where
$X_u^P=1$ means that the patch site is active at time~$u$. Its initial value is
\begin{equation*}
X_s^P
=
\pw{1}{\mathsf X(P)=\mathsf I}
{\ind\cb{\alpha(P)=\beta}}{\mathsf X(P)=\mathsf O}.
\end{equation*}
The interior marks are then read in chronological order. When~$X_{u-}^P=1$, a
$\delta$-mark sets~$X_u^P=0$ and a~$\beta$-mark leaves~$X_u^P=1$; when
$X_{u-}^P=0$, either mark leaves~$X_u^P=0$. Between marks,~$X^P$ is constant.
For a finite patch with terminal label~$\mathsf E$, set~$X_e^P=X_{e-}^P$.

% ============================================================================
% BLURB C. Consistency with the fixed skeleton
%
% Reader question:
%   Which realizations of the local marks are compatible with the patch
%   boundaries already fixed by the skeleton?
%
% Job:
%   Explain consistency in prose before the formula. Every interior
%   nonempty-target mark must occur while the patch site is inactive; otherwise
%   it would create an additional successful interaction inside the patch. If
%   the terminal boundary is outgoing, the site must be active immediately
%   before the endpoint so that the recorded outgoing interaction is
%   successful. These conditions can force the hidden initial interaction kind
%   or exclude a death, depending on the boundary type. Incoming and end
%   terminal boundaries impose no analogous terminal condition.
%
%   Define Con(P), then the consistent patch law P_P^con and expectation
%   E_P^con. State cleanly that for patches arising from the graphical
%   construction the consistency probability is positive almost surely. Make
%   clear that P_P^con is simply the reference one-patch law conditioned on
%   producing the prescribed patch boundaries.
%
%   Insert paper/sections/patch-interior-figure here. Rewrite its caption around
%   the mathematical content: the left object in each row gives the prescribed
%   patch boundaries and the objects to the right give the possible realizations
%   of the one-site process X^P compatible with them. Avoid implementation
%   language such as "striped prism" and "gray volume" except where genuinely
%   needed to decode the picture.
% ============================================================================

% ============================================================================
% BLURB D. Conditional factorization
%
% Reader question:
%   Why do the consistent one-patch laws describe the actual graphical
%   construction independently once G_T is fixed?
%
% Job:
%   State the mechanism before the theorem. Conditioning on G_T fixes the patch
%   boundaries. Before compatibility is imposed, the omitted Poisson marks live
%   on disjoint patch intervals and are independent, with the reference laws
%   from Blurb B. The event that the prescribed skeleton occurs is exactly the
%   intersection of the patchwise consistency events from Blurb C. Therefore
%   conditioning replaces each reference law by its consistent law without
%   coupling different patches.
%
%   State Theorem [Patch factorization] and equation (patch-factorization).
%   Keep the theorem at full strength, including the integrable signed case.
%
% Proof organization:
%   Keep a short main-text proof that makes the preceding product-conditioning
%   mechanism mathematically explicit. Move the detailed standard-Borel,
%   ordered-record, Mecke, m_T(dg), and Radon--Nikodym disintegration argument
%   from the preserved version to an appendix. The appendix should verify
%   rigorously that the fixed record is the complete skeleton iff every patch is
%   consistent and derive the conditional product law.
% ============================================================================

% ============================================================================
% BLURB E. Patch contributions and proof of the representation
%
% Reader question:
%   What local quantity is averaged on each patch, and how do these local
%   averages reconstruct the Feynman--Kac dual representation?
%
% Job:
%   Define the local sign sigma(P). For a bulk patch define F(P) as its local
%   sign times its local Feynman--Kac weight and C(P) as the expectation under
%   the consistent patch law. For an end patch define F(z,P) by including the
%   terminal factor z^{X_t^P}, and define C(z,P). Explain before or alongside
%   the formulas what these quantities mean. State that z -> C(z,P) is affine.
%   Keep explicit boundary-type formulas in the appendix.
%
%   Then prove Theorem~\ref{thm:patch-representation}. On the graphical space,
%   split the global dual random variable into:
%       (1) the product of the patch signs,
%       (2) the sum of the patch Feynman--Kac integrals,
%       (3) the terminal monomial over the end patches.
%   Combine these into the pathwise patch factorization. Condition on G_t, apply
%   patch factorization, then take expectation and use monomial duality.
%
% Main-text principle:
%   The reader should see here exactly why the entire construction pays off.
%   Keep the structural identities; defer only routine local calculations.
% ============================================================================

% ============================================================================
% BLURB F. Initial laws and retained cancellation
%
% Reader question:
%   What does the representation become after averaging over the initial
%   configuration, and what cancellation has the patch construction preserved?
%
% Job:
%   Derive the general-initial-law assertion of
%   Theorem~\ref{thm:patch-representation}. Then specialize to Bernoulli product
%   measures using the fact that distinct end patches have distinct sites and
%   that C(z,P) is affine, obtaining equation
%   (patch-representation-product-law).
%
%   Finish with the conceptual interpretation. For a fixed skeleton, all local
%   graphical histories compatible with one patch are averaged inside C(P) or
%   C(z,P). Thus cancellations between different compatible local histories are
%   retained before any later absolute-value estimate is made. Avoid vague
%   phrases such as "groups the signed dual trajectory".
%
% Formatting:
%   No paragraph headings are needed. Keep FloatBarrier only if it is still
%   required by the relocated patch-interior figure.
% ============================================================================
