\subsection{Successful-interaction skeleton and patches}
\label{sec:patches}

% Previous version preserved in
% paper/revision-reference/section-4-3-successful-interaction-skeleton-and-patches.tex.
%
% The figure source paper/figures/patch-geometry.tex is unchanged. In the
% rewrite, the figure should appear only after the finite-horizon patch family
% has been defined.

% ============================================================================
% BLURB A. Why retain a successful-interaction skeleton
%
% Reader question:
%   What information from the full graphical construction should we condition
%   on so that all interactions between sites are fixed while local randomness
%   remains available to average?
%
% Job:
%   Motivate the skeleton as coarse conditioning information. Explain that an
%   actual interaction between distinct sites occurs exactly when a nonempty-
%   target mark rings at an active source. To determine such an interaction it
%   is enough to retain its source, time, and target. Deliberately forget its
%   kind: for fixed source/time/target, birth and split involve the same sites
%   and differ only in the resulting state of the source. Also omit deaths and
%   inactive-source rings. Once these records are fixed, their times divide each
%   site line into intervals within which the omitted randomness is local.
%
% Keep out:
%   The formal definition of I_T, the patch labels, and the finite-horizon
%   truncation. Do not claim a formal minimality theorem for the sigma-algebra.
% ============================================================================

We now choose coarse~$\sigma$-algebra for the graphical construction on which to
condition. It should determine every interaction between distinct sites while
leaving local randomness unobserved. An interaction between sites
occurs exactly when a mark with nonempty target rings at an active source. To
specify such an interaction, it is enough to retain its source, time, and
target. We do not retain its kind~$\alpha$: for fixed~$(i,t,S)$, a birth and a
split involve the same source and target sites and differ only in the resulting
state of the source. We also omit deaths, whose target is empty, and rings at
inactive sources, which do not change the process at all. The retained records forms
the successful-interaction skeleton. Once this skeleton is fixed, its
interaction times divide each site line in spacetime into consecutive intervals, called
patches, within which the omitted randomness is local.

% ============================================================================
% BLURB B. Successful interactions, skeleton, and conditioning information
%
% Reader question:
%   Exactly which marks are retained, what does the skeleton remember and
%   forget, and what information will be conditioned on later?
%
% Job:
%   Define a successful positive-time interaction as
%       S != empty and i in A_{t-}.
%   Declare the deterministic initial interaction successful as well. Define its
%   record and the record (i,t,S) of a positive-time successful interaction.
%   Emphasize that the record deliberately forgets alpha: it remembers source,
%   time, and target, but not whether the mark was a split or a birth. Deaths
%   and inactive-source rings are also omitted.
%
%   Define I_T, I, and G_T = sigma(Y_0,I_T). State explicitly that G_T is the
%   information on which we condition in the next subsection. Use the local
%   finiteness from the graphical construction to note that I_T is finite a.s.
%
% Language:
%   Use "positive-time successful interactions", not "ordinary" interactions.
%   No paragraph heading is needed.
% ============================================================================

\begin{definition}[Successful interaction and its record]
\label{def:successful-interaction}
The deterministic initial interaction~$\iota_0$ from
\eqref{eq:initial-interaction} is declared successful and has record
\begin{equation*}
(\infty,0,A_0).
\end{equation*}
An interaction~$(i,t,\alpha,S)\in I^{\mathrm P}$ is
\emph{successful} when
\begin{equation*}
S\neq\varnothing
\qquad\text{and}\qquad
i\in A_{t-}.
\end{equation*}
Its successful-interaction record is~$(i,t,S)$. Thus the record remembers the
source, time, and target of the interaction, but not its kind~$\alpha$.
\end{definition}

For~$T<\infty$, let~$\mathcal I_T$ be the successful-interaction skeleton up to
time~$T$:
\begin{equation*}
\mathcal I_T
=
\cb{(\infty,0,A_0)}
\cup
\cb{(i,t,S):t\leq T\text{ and, for some }\alpha\in\cb{\beta,\delta},
(i,t,\alpha,S)\text{ is successful}}.
\end{equation*}
Thus~$\mathcal I_T$ omits deaths and rings at inactive sources, as well as the
kind of each retained interaction. Set
\begin{equation*}
\mathcal I=\bigcup_{T<\infty}\mathcal I_T,
\qquad
\mathcal G_T=\sigma\bb{Y_0,\mathcal I_T}.
\end{equation*}
The~$\sigma$-algebra~$\mathcal G_T$ is the information on which we condition in
the next subsection. The local-finiteness statement in
Section~\ref{subsec:dual-graphical-construction} implies that~$\mathcal I_T$ is
finite almost surely.

% ============================================================================
% BLURB C. Patch geometry and boundary labels
%
% Reader question:
%   Given the successful-interaction skeleton, what is a patch geometrically,
%   and what do the I/O/E labels mean?
%
% Job:
%   Give the geometric picture first: on each site line, successful interactions
%   cut time into consecutive intervals. Each interval starts at an incoming or
%   outgoing successful interaction and ends at the next successful interaction
%   involving that site, if one exists.
%
%   Distinguish positive-time records from the initial record: a positive-time
%   record is outgoing at its source and incoming at every target; the initial
%   record supplies only the incoming time-zero boundaries at sites in A_0.
%
%   Then define T^I(i), T^O(i), T(i), and e_i(s). Define a patch P and its data
%   i(P), [s(P),e(P)), S(P), and boundary labels X(P),Y(P). Explain in prose
%   before or immediately after the formulas:
%       I = incoming interaction,
%       O = outgoing interaction,
%       E = no later successful interaction on that site line.
%   Define the full family P and state that an E-terminal patch in the infinite
%   family is infinite, while I/O-terminal patches are finite.
%
% Formatting:
%   Keep the formal patch definition environment. A compact table may be kept
%   as a glossary if it still improves readability, but it should not merely
%   duplicate unexplained formulas. Do not use a \paragraph heading.
% ============================================================================

% ============================================================================
% BLURB D. Finite-horizon patches and the figure
%
% Reader question:
%   At a finite observation time t, which patches are complete, which are cut by
%   the horizon, and how should the reader see the whole construction?
%
% Job:
%   Motivate the truncation from the finite-time Feynman--Kac representation.
%   For patches already ended by t, keep the completed patch. For the unique
%   open interval on each relevant site line, cut it at t and give it terminal
%   label E. Define P^{downarrow t}, B_t, E_t, and P_t. State finiteness and the
%   fact that distinct end patches have distinct sites.
%
%   Only after these objects are defined, insert Figure~\ref{fig:patch-geometry}.
%   The figure should compare:
%       left: the full graphical realization;
%       right: the induced finite-horizon patch decomposition.
%   The surrounding prose should already have defined bulk/end patches and the
%   I/O/E labels, so the caption can be substantially shorter. Decode only the
%   genuinely visual conventions: active shading, successful versus omitted
%   marks, patch sides/boundaries, and the bulk/end separator. Avoid the phrase
%   "XO type patches".
%
%   Keep \FloatBarrier after the figure. Finish with the extension notation
%   P^{uparrow t} if it is still needed here; introduce it in one sentence that
%   says it extends an end patch to a later time without changing its initial
%   data.
%
% Figure source:
%   Do not modify paper/figures/patch-geometry.tex during the prose rewrite
%   unless the rendered figure itself later proves misleading.
% ============================================================================
