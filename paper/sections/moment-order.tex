\subsection{Centered-moment order}
\label{sec:moment-order}
\label{subsec:moment-monotonicity}

We now prove part~\textup{(ii)} of Theorem~\ref{thm:patch-positivity-order}.
We write the generator in the centered-monomial basis and show that its
off-diagonal coefficients are nonnegative. The corresponding equations for the
centered moments then form a row-finite Metzler system, so the standard
positivity theorem for Metzler generators~\cite{ArendtEtAl1986} gives
preservation of nonnegative centered moments.

If~$i\notin A$, the update at~$i$ leaves~$\chi_A^*$ unchanged. If~$i\in A$,
then
\begin{equation*}
\lambda_i\bb{E_{i,\rho_i}-I}\chi_A^*
=
\lambda_i\bb{\rho_i-p_i^\star}\chi_{A\setminus\cb{i}}^*
-
\lambda_i\chi_A^*.
\end{equation*}
Expanding~$\lambda_i\rho_i$ and~$\lambda_i$ in monomials and summing over
$i\in A$ gives
\begin{equation*}
\Lcal\chi_A^*
=\sum_{i\in A}\Cb{
-\lambda_i(\mb 0)\chi_A^*
+
\sum_{S\subseteq N(i)}
\bb{
\widehat{\lambda_i\rho_i}(S)
-p_i^\star\widehat{\lambda_i}(S)
}
\chi_S\chi_{A\setminus\cb{i}}^*
+
\sum_{\substack{S\subseteq N(i)\\S\neq\vn}}
\bb{-\widehat{\lambda_i}(S)}
\chi_S\chi_A^*
}.
\end{equation*}
Part~\textup{(i)} gives
\begin{equation*}
-\widehat{\lambda_i}(S)\geq0
\quad\text{for }S\neq\vn,
\qquad
\widehat{\lambda_i\rho_i}(S)
-p_i^\star\widehat{\lambda_i}(S)
\geq0
\quad\text{for every }S\subseteq N(i).
\end{equation*}
For the second inequality, the case~$S=\vn$ is
$\lambda_i(\mb 0)(\rho_i(\mb 0)-p_i^\star)\geq0$. If~$S\neq\vn$ and
$\widehat{\lambda_i}(S)<0$, it follows from the definition of~$p_i^\star$; if
$\widehat{\lambda_i}(S)=0$, it follows from the second coefficient inequality
in part~\textup{(i)}.

It remains to expand the products~$\chi_S\chi_B^*$ in the centered basis. For
finite~$S,B\subseteq\Lambda$,
\begin{equation*}
\chi_S\chi_B^*
=
\bb{\prod_{j\in S\cap B}\bb{1-p_j^\star}}
\sum_{R\subseteq S}
\bb{\prod_{j\in S\setminus R}p_j^\star}
\chi_{R\cup(B\setminus S)}^*.
\end{equation*}
Indeed, first use
$\eta(j)(\eta(j)-p_j^\star)=(1-p_j^\star)\eta(j)$ on~$S\cap B$, and then
expand~$\eta(j)=p_j^\star+(\eta(j)-p_j^\star)$ for~$j\in S$. Since
$0\leq p_j^\star\leq1$, every coefficient is nonnegative. Hence, after
collecting equal centered monomials,
\begin{equation*}
\Lcal\chi_A^*
=
\sum_{B\Subset\Lambda}M(A,B)\chi_B^*,
\qquad
M(A,B)\geq0\quad\text{for }B\neq A,
\end{equation*}
and for each~$A$ only finitely many coefficients~$M(A,B)$ are nonzero.

Let~$\sigma$ be a finite signed measure and put
$m_A(t)=(\sigma P_t)(\chi_A^*)$. The backward equation gives
\begin{equation*}
\frac{d}{dt}m_A(t)
=
\sum_{B\Subset\Lambda}M(A,B)m_B(t).
\end{equation*}
This is a row-finite Metzler system. By the positivity result cited above,
$m_A(0)\geq0$ for every finite~$A$ implies~$m_A(t)\geq0$ for every finite~$A$
and~$t\geq0$. Taking~$\sigma=\nu-\mu$ therefore proves
\begin{equation*}
\mu\preceq_*\nu
\quad\Longrightarrow\quad
\mu P_t\preceq_*\nu P_t
\qquad\text{for every }t\geq0.
\end{equation*}

The same argument with~$\sigma=\mu$ shows that~$\mathcal M_*$ is preserved by
the semigroup. Ordinary monomial comparison follows from expansion
\begin{equation*}
\chi_A
=
\sum_{B\subseteq A}
\bb{\prod_{i\in A\setminus B}p_i^\star}\chi_B^*,
\end{equation*}
whose coefficients are nonnegative. Finally, every nonempty centered moment of
$\mu_{\mb p^\star}$ is zero, so
$\mu_{\mb p^\star}\preceq_*\mu$ for every~$\mu\in\mathcal M_*$. Applying the
monomial comparison gives
\begin{equation*}
(\mu_{\mb p^\star}P_t)(\chi_A)
\leq
(\mu P_t)(\chi_A).
\end{equation*}
This proves part~\textup{(ii)} and completes the proof of
Theorem~\ref{thm:patch-positivity-order}.

\subsection{Pure-death comparison}
\label{subsec:pure-death-comparison}

% ============================================================================
% BLURB A. Comparison mechanism
%
% Reader question:
%   Why does removing independent 1->0 transitions lead to a comparison, and
%   where are the patch representation and the class M_* used?
%
% Job:
%   Start from Corollary~\ref{cor:pure-death-comparison}. The two systems differ
%   only by environment-independent 1->0 transitions. Their nonempty
%   interaction part is therefore the same, so after conditioning on the
%   recorded successful interactions the two patch representations use the same
%   patch geometry.
%
%   Explain the only obstruction to an immediate factor-by-factor comparison.
%   Bulk patch contributions are nonnegative by patch positivity. End-patch
%   factors depend on the terminal spins and need not be nonnegative at both
%   spin values, so they cannot simply be compared pointwise and multiplied.
%   The condition mu in M_* is precisely what will control these terminal
%   factors after centering at p^star.
%
% Reader takeaway:
%   This subsection combines the two structures proved earlier: patch
%   positivity controls the bulk, while nonnegative centered moments control the
%   terminal part.
%
% Keep out:
%   Do not introduce psi_i, Mobius coefficients, or local rate calculations in
%   this opening paragraph.
% ============================================================================

% ============================================================================
% BLURB B. Centered end factors
%
% Reader question:
%   How does membership in M_* make the terminal patch contribution positive?
%
% Job:
%   For an end patch P based at i, use that z -> C(z,P) is affine,
%   nondecreasing, and nonnegative at p_i^star to write
%       C(z,P)
%       = C(p_i^star,P) + kappa(P)(z-p_i^star),
%       C(p_i^star,P) >= 0,  kappa(P) >= 0.
%
%   Distinct end patches are based at distinct sites. Expand the product of end
%   factors over subsets Q of E_t. The resulting formula should express
%       mu(prod_{P in E_t} C(eta(i(P)),P))
%   as a sum of centered moments
%       mu(chi_{ {i(P): P in Q} }^*)
%   multiplied by products of C(p_i^star,P) and kappa(P). Retain the old label
%   eq:centered-end-expansion because it is useful in the comparison below.
%
%   Since every coefficient is nonnegative, the whole end-factor average is
%   nonnegative whenever mu is in M_*.
%
% Reader takeaway:
%   M_* is the exact hypothesis that turns the potentially signed terminal
%   product into a positive linear combination.
%
% Keep out:
%   Do not compare primed and unprimed systems yet. That comparison requires the
%   next blurb, where the primed system is shown to have the same p^star.
% ============================================================================

% ============================================================================
% BLURB C. Removing pure deaths
%
% Reader question:
%   What changes in the patch representation when the independent 1->0
%   transitions are removed?
%
% Job:
%   Rewrite the generator change in the current notation:
%       lambda_i' = lambda_i - d_i,
%       lambda_i' rho_i' = lambda_i rho_i.
%   Use the coefficient criterion from part (i) to show briefly that L' remains
%   patch-positive. The nonconstant Mobius coefficients relevant to the
%   threshold are unchanged, so the threshold formula gives the same p_i^star.
%   Handle the degenerate case lambda_i'(0)=0 using the already established
%   zero-constant-term observation rather than reproducing the old long case
%   split.
%
%   Then explain the dynamic effect inside a patch. With no incoming
%   interaction, the primed one-site dynamics is obtained by deleting some
%   1->0 transitions. Reintroduce only the one-site relaxation psi_i needed for
%   the patch formulas and show
%       psi_i'(u,z) >= psi_i(u,z),
%   while its slope in z also increases.
%
%   Cite the explicit patch formulas to conclude
%       C(P) <= C'(P)
%   for bulk patches and, for every end patch based at i,
%       C(p_i^star,P) <= C'(p_i^star,P),
%       kappa(P) <= kappa'(P).
%
% Reader takeaway:
%   Removing pure deaths leaves the interaction skeleton unchanged and enlarges
%   every nonnegative local coefficient used in the patch comparison.
%
% Keep out:
%   Avoid the old c_i^0,c_i^1,r_i notation and do not introduce V_i or
%   varphi_i unless an explicit patch formula genuinely forces it.
% ============================================================================

% ============================================================================
% BLURB D. Comparison and invariant measures
%
% Reader question:
%   How do the local inequalities give the semigroup comparison?
%
% Job:
%   Fix the common successful-interaction skeleton. Every bulk factor for L is
%   nonnegative and no larger than its primed counterpart. By the centered
%   end-factor expansion from Blurb B, the terminal average is a sum of
%   centered moments of mu with nonnegative coefficients. Since mu is in M_*,
%   every term is nonnegative, and the inequalities from Blurb C show that each
%   coefficient is no larger than the corresponding primed coefficient.
%
%   Therefore the contribution of every fixed skeleton for L is no larger than
%   the corresponding contribution for L'. Average over the common skeleton and
%   invoke Theorem~\ref{thm:patch-representation} to obtain
%       (mu P_t)(chi_A) <= (mu P_t')(chi_A).
%   This proves Corollary~\ref{cor:pure-death-comparison}.
%
%   Afterwards retain the invariant-measure comparison as a short corollary.
%   Under uniqueness of invariant measures for both systems, apply the
%   pure-death comparison to mu_{1}, average over t in [0,T], and let T tend to
%   infinity. Cesaro subsequential limits are invariant, so uniqueness
%   identifies them.
%
% Reader takeaway:
%   Once one conditions on the common interaction skeleton, the comparison is
%   termwise; no monotone coupling of the original spin systems is required.
% ============================================================================
