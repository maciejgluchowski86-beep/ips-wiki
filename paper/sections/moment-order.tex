\subsection{Centered-moment order}
\label{sec:moment-order}
\label{subsec:moment-monotonicity}

We now prove part~\textup{(ii)} of Theorem~\ref{thm:patch-positivity-order}.
We write the generator in the centered-monomial basis and show that its
off-diagonal coefficients are nonnegative. The corresponding equations for the
centered moments then form a row-finite Metzler system, so the standard
positivity theorem for Metzler generators~\cite{ArendtEtAl1986} gives
preservation of nonnegative centered moments.

If~$i\notin A$, the update at~$i$ leaves~$\chi_A^*$ unchanged. If~$i\in A$,
then
\begin{equation*}
\lambda_i\bb{E_{i,\rho_i}-I}\chi_A^*
=
\lambda_i\bb{\rho_i-p_i^\star}\chi_{A\setminus\cb{i}}^*
-
\lambda_i\chi_A^*.
\end{equation*}
Expanding~$\lambda_i\rho_i$ and~$\lambda_i$ in monomials and summing over
$i\in A$ gives
\begin{equation*}
\Lcal\chi_A^*
=\sum_{i\in A}\Cb{
-\lambda_i(\mb 0)\chi_A^*
+
\sum_{S\subseteq N(i)}
\bb{
\widehat{\lambda_i\rho_i}(S)
-p_i^\star\widehat{\lambda_i}(S)
}
\chi_S\chi_{A\setminus\cb{i}}^*
+
\sum_{\substack{S\subseteq N(i)\\S\neq\vn}}
\bb{-\widehat{\lambda_i}(S)}
\chi_S\chi_A^*
}.
\end{equation*}
Part~\textup{(i)} gives
\begin{equation*}
-\widehat{\lambda_i}(S)\geq0
\quad\text{for }S\neq\vn,
\qquad
\widehat{\lambda_i\rho_i}(S)
-p_i^\star\widehat{\lambda_i}(S)
\geq0
\quad\text{for every }S\subseteq N(i).
\end{equation*}
For the second inequality, the case~$S=\vn$ is
$\lambda_i(\mb 0)(\rho_i(\mb 0)-p_i^\star)\geq0$. If~$S\neq\vn$ and
$\widehat{\lambda_i}(S)<0$, it follows from the definition of~$p_i^\star$; if
$\widehat{\lambda_i}(S)=0$, it follows from the second coefficient inequality
in part~\textup{(i)}.

It remains to expand the products~$\chi_S\chi_B^*$ in the centered basis. For
finite~$S,B\subseteq\Lambda$,
\begin{equation*}
\chi_S\chi_B^*
=
\bb{\prod_{j\in S\cap B}\bb{1-p_j^\star}}
\sum_{R\subseteq S}
\bb{\prod_{j\in S\setminus R}p_j^\star}
\chi_{R\cup(B\setminus S)}^*.
\end{equation*}
Indeed, first use
$\eta(j)(\eta(j)-p_j^\star)=(1-p_j^\star)\eta(j)$ on~$S\cap B$, and then
expand~$\eta(j)=p_j^\star+(\eta(j)-p_j^\star)$ for~$j\in S$. Since
$0\leq p_j^\star\leq1$, every coefficient is nonnegative. Hence, after
collecting equal centered monomials,
\begin{equation*}
\Lcal\chi_A^*
=
\sum_{B\Subset\Lambda}M(A,B)\chi_B^*,
\qquad
M(A,B)\geq0\quad\text{for }B\neq A,
\end{equation*}
and for each~$A$ only finitely many coefficients~$M(A,B)$ are nonzero.

Let~$\sigma$ be a finite signed measure and put
$m_A(t)=(\sigma P_t)(\chi_A^*)$. The backward equation gives
\begin{equation*}
\frac{d}{dt}m_A(t)
=
\sum_{B\Subset\Lambda}M(A,B)m_B(t).
\end{equation*}
This is a row-finite Metzler system. By the positivity result cited above,
$m_A(0)\geq0$ for every finite~$A$ implies~$m_A(t)\geq0$ for every finite~$A$
and~$t\geq0$. Taking~$\sigma=\nu-\mu$ therefore proves
\begin{equation*}
\mu\preceq_*\nu
\quad\Longrightarrow\quad
\mu P_t\preceq_*\nu P_t
\qquad\text{for every }t\geq0.
\end{equation*}

The same argument with~$\sigma=\mu$ shows that~$\mathcal M_*$ is preserved by
the semigroup. Ordinary monomial comparison follows from expansion
\begin{equation*}
\chi_A
=
\sum_{B\subseteq A}
\bb{\prod_{i\in A\setminus B}p_i^\star}\chi_B^*,
\end{equation*}
whose coefficients are nonnegative. Finally, every nonempty centered moment of
$\mu_{\mb p^\star}$ is zero, so
$\mu_{\mb p^\star}\preceq_*\mu$ for every~$\mu\in\mathcal M_*$. Applying the
monomial comparison gives
\begin{equation*}
(\mu_{\mb p^\star}P_t)(\chi_A)
\leq
(\mu P_t)(\chi_A).
\end{equation*}
This proves part~\textup{(ii)} and completes the proof of
Theorem~\ref{thm:patch-positivity-order}.

\subsection{Pure-death comparison}
\label{subsec:pure-death-comparison}

Let~$\Lcal$ and~$\Lcal'$ be as in Corollary~\ref{cor:pure-death-comparison}.
As observed in Section~\ref{subsec:signed-set-process}, adding the pure-death
term to~$\Lcal'$ leaves the signed dual jump process unchanged and only lowers
its Feynman--Kac potential. Hence the two systems have the same
successful-interaction skeleton and the same patches, and it remains to compare
their patch contributions.

% ============================================================================
% BLURB B. Centered end-factor expansion
%
% Reader question:
%   What algebraic form of the end factors will be used in the comparison?
%
% Job:
%   Do the calculation directly, without another motivational paragraph. For an
%   end patch P based at i, use affinity and the threshold property to write
%       C(z,P)
%       = C(p_i^star,P) + kappa(P)(z-p_i^star),
%       C(p_i^star,P) >= 0,  kappa(P) >= 0.
%   Since distinct end patches are based at distinct sites, expand the product
%   over E_t into centered moments of mu. Retain the old label
%   eq:centered-end-expansion because Blurb D will use the formula explicitly.
%   Conclude only that every term is nonnegative for mu in M_*.
%
% Keep out:
%   Do not explain again why M_* is the right class. Do not introduce the primed
%   system here; the same expansion will apply to it after Blurb C proves that
%   its threshold profile is the same.
% ============================================================================

% ============================================================================
% BLURB C. Compare the patch contributions
%
% Reader question:
%   How does removing pure deaths change the coefficients in the common patch
%   representation?
%
% Job:
%   First dispose of the structural bookkeeping in the current notation:
%       lambda_i' = lambda_i - d_i,
%       lambda_i' rho_i' = lambda_i rho_i.
%   Use part (i) to show briefly that L' is patch-positive and that the threshold
%   formula gives the same p_i^star. Handle lambda_i'(0)=0 with the existing
%   zero-constant-term observation; do not reproduce the old c_i^0,c_i^1 case
%   split.
%
%   Then compare the actual patch contributions. The signed dual jump process,
%   the successful-interaction skeleton, and the consistent patch laws are the
%   same for L and L'; the only change is the smaller Feynman--Kac tilt for L.
%   Use the explicit one-site/patch formulas from Section 5.1 and the appendix to
%   prove exactly the inequalities needed below:
%       C(P) <= C'(P),
%       C(p_i^star,P) <= C'(p_i^star,P),
%       kappa(P) <= kappa'(P).
%   Reintroduce psi_i only if it gives the shortest transparent verification of
%   these inequalities.
%
% Reader takeaway:
%   The two representations have the same random patch structure; removing
%   deaths increases the nonnegative coefficients that remain after patch
%   averaging.
%
% Keep out:
%   Avoid V_i, varphi_i, and other Section 4 notation unless an explicit formula
%   genuinely needs it. Do not re-explain the construction of the skeleton or
%   the consistent patch law.
% ============================================================================

% ============================================================================
% BLURB D. Finish the comparison
%
% Reader question:
%   How do the patchwise inequalities imply the semigroup comparison?
%
% Job:
%   Condition on the common successful-interaction skeleton and use the general
%   patch representation. Every bulk factor is nonnegative and bounded by the
%   corresponding primed factor. Expand the end-factor expectation using
%   eq:centered-end-expansion. For mu in M_*, every centered moment in that sum
%   is nonnegative, while Blurb C shows that each coefficient is bounded by its
%   primed counterpart. Therefore the conditional contribution for L is bounded
%   by the conditional contribution for L'. Average over the common skeleton to
%   obtain
%       (mu P_t)(chi_A) <= (mu P_t')(chi_A),
%   proving Corollary~\ref{cor:pure-death-comparison}.
%
%   After the proof, retain the invariant-measure comparison as a short
%   corollary. Apply the comparison to mu_1, take Cesaro averages, and use
%   uniqueness to identify the invariant limits.
%
% Keep out:
%   No further discussion of monotone couplings or of why centered moments were
%   introduced. At this point the conclusion should be nearly mechanical.
% ============================================================================
