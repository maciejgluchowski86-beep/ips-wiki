\subsection{Nonnegative patch weights and centered end factors}
\label{subsec:m-star}

% This subsection resumes the proof of
% Theorem~\ref{thm:patch-positivity-order}, part~\textup{(ii)}. The reader has
% just seen the coefficient criterion, threshold formula, and their dynamic
% interpretation. Return now to the finite-time patch representation.
%
% Main job:
%   Use the threshold from part (i) to write each end contribution as a
%   nonnegative constant plus a nonnegative multiple of
%   eta(i)-p_i^star. Expand the product over end patches in the centered-monomial
%   basis. Together with patch positivity, conclude that the coefficients
%   associated with a fixed successful-interaction skeleton are nonnegative.
%
% Reader takeaway:
%   Part (i) supplies exactly the sign information needed to make the patch
%   representation compatible with centered moments.
%
% Placeholder. Rewrite subsection blurb by blurb.

\subsection{Centered-moment order}
\label{sec:moment-order}
\label{subsec:moment-monotonicity}

% This subsection completes part~\textup{(ii)} of
% Theorem~\ref{thm:patch-positivity-order} and therefore completes the proof of
% the theorem.
%
% Main job:
%   Explain why the patch expansion already gives ordinary-monomial comparison
%   but does not by itself prove preservation of centered-moment order. Use the
%   generator in the centered-monomial basis, prove nonnegativity of its
%   off-diagonal coefficients, pass from finite volume to Lambda, and conclude
%       mu \preceq_* nu  =>  mu P_t \preceq_* nu P_t.
%   Derive preservation of M_* and ordinary-monomial comparison as consequences.
%   End explicitly by saying that part (ii), and hence
%   Theorem~\ref{thm:patch-positivity-order}, is proved.
%
% Product-profile consequences, if retained, belong here after the theorem has
% been completed, rather than as a separate stage of the proof.
%
% Placeholder. Rewrite subsection blurb by blurb.

\subsection{Pure-death comparison}
\label{subsec:pure-death-comparison}

% Theorem~\ref{thm:patch-positivity-order} should already be complete before
% this subsection begins. Present the pure-death comparison as a consequence of
% the theorem and the patch representation, not as another part of its proof.
%
% Placeholder. Rewrite subsection blurb by blurb.
