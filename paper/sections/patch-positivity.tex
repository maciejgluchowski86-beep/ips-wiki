\section{Proof of Theorem~\ref{thm:patch-positivity-order}}
\label{sec:patch-positivity}

This section proves Theorem~\ref{thm:patch-positivity-order}. The first two
subsections prove part~\textup{(i)}: we first characterize patch positivity in
terms of the M\"obius coefficients of~$\lambda_i$ and~$\lambda_i\rho_i$, and
then compute the patch threshold profile. After interpreting the criterion, we
return to the patch representation to prove part~\textup{(ii)}, preservation of
the centered-moment order. The remaining subsections give consequences of the
theorem. The starting point is the representation from
Theorem~\ref{thm:patch-representation}:
\begin{equation*}
(\mu P_t)(\chi_A)
=
\E_A\Cb{
\prod_{P\in\mathcal B_t}C(P)\,
\mu\bb{
\prod_{P\in\mathcal E_t}C\bb{\eta\bb{i(P)},P}
}
}.
\end{equation*}
The patch contributions appearing here are defined in
Section~\ref{sec:representation}. Patch positivity means that, for every
$A\Subset\Lambda$, almost surely every patch~$P$ in the full patch
family~$\mathcal P$ satisfies~$C(P)\geq0$. The end contributions require an
additional sign control because~$C(z,P)$ still depends on its terminal
argument~$z$.

\subsection{Coefficient criterion}
\label{subsec:positivity-criterion}

We begin the proof of part~\textup{(i)} of
Theorem~\ref{thm:patch-positivity-order}. Fix a site~$i$ and a patch
$P\in\mathcal P$ based at~$i$, and write
$\Delta=e(P)-s(P)\in(0,\infty]$. The initial label
$\mathsf X(P)\in\cb{\mathsf I,\mathsf O}$ records whether the interaction at
$s(P)$ is incoming or outgoing at~$i$. The terminal label
$\mathsf Y(P)\in\cb{\mathsf I,\mathsf O,\mathsf E}$ records whether the next
successful interaction is incoming or outgoing, with~$\mathsf E$ meaning that
there is no later successful interaction at that site. The contributions~$C(P)$
for~$\mathsf{XI}$ and~$\mathsf{XE}$ patches are the same, so there are four
distinct cases:~$\cb{\mathsf{II},\mathsf{IE}}$,~$\mathsf{IO}$,
$\cb{\mathsf{OI},\mathsf{OE}}$, and~$\mathsf{OO}$. We use throughout
\begin{equation*}
\delta_i(S)\sigma_i^\delta(S)
=\widehat{\lambda_i\rho_i}(S),
\qquad
\beta_i(S)\sigma_i^\beta(S)
=-\widehat{\lambda_i}(S),
\end{equation*}
from Appendix~\ref{app:bulk-formulas}.

If the initial boundary is incoming, the full-patch formulas give
\begin{equation*}
C(P)
=
\begin{cases}
\displaystyle
\frac{\psi_i(\Delta,1)}{\varphi_i(\Delta)},
&\mathsf X(P)\mathsf Y(P)\in\cb{\mathsf{II},\mathsf{IE}},
\\[1em]
e^{V_i\Delta},
&\mathsf X(P)\mathsf Y(P)=\mathsf{IO}.
\end{cases}
\end{equation*}
Since~$\delta_i(\vn)
=\widehat{\lambda_i\rho_i}(\vn)
=\lambda_i(\mb 0)\rho_i(\mb 0)\geq0$,
we have~$\psi_i(\Delta,1)\geq0$ and~$\varphi_i(\Delta)>0$. The exponential is of course positive. Thus every patch with an
incoming initial boundary has a nonnegative contribution. Only patches with an
outgoing initial boundary impose conditions for patch-positivity.

Suppose that the initial boundary is outgoing, with nonempty target~$S$. If the
terminal boundary is also outgoing, then
\begin{equation*}
C(P)=\sigma_i^\beta(S)e^{V_i\Delta},
\qquad
\beta_i(S)\sigma_i^\beta(S)=-\widehat{\lambda_i}(S).
\end{equation*}
Consequently, all~$\mathsf{OO}$ contributions are nonnegative exactly when
\begin{equation}
\label{eq:patch-positivity-first-inequality}
\widehat{\lambda_i}(S)\leq0
\qquad\text{for every nonempty }S\subseteq N(i).
\end{equation}

The remaining outgoing-initial cases,~$\mathsf{OI}$ and~$\mathsf{OE}$, have
the same contribution formula. Its denominator is positive, while its numerator
is
\begin{equation*}
\widehat{\lambda_i\rho_i}(S)
-\widehat{\lambda_i}(S)\psi_i(\Delta,1).
\end{equation*}
The explicit expression for~$\psi_i$ from Appendix~\ref{app:positivity-algebra} is
\begin{equation*}
\psi_i(\Delta,1)
=\rho_i(\mb 0)
+\bb{1-\rho_i(\mb 0)}e^{-\lambda_i(\mb 0)\Delta},
\end{equation*}
so the numerator can be written as
\begin{equation}
\label{eq:outgoing-incoming-numerator}
\widehat{\lambda_i\bb{\rho_i-\rho_i(\mb 0)}}(S)
+
\bb{1-\rho_i(\mb 0)}
\bb{-\widehat{\lambda_i}(S)}
e^{-\lambda_i(\mb 0)\Delta}.
\end{equation}
Under~\eqref{eq:patch-positivity-first-inequality}, the second term is
nonnegative. If~$\lambda_i(\mb 0)>0$, letting~$\Delta\to\infty$ shows that the
$\mathsf{OI}/\mathsf{OE}$ contributions are nonnegative for every patch length
exactly when
\begin{equation}
\label{eq:patch-positivity-second-inequality}
\widehat{\lambda_i\bb{\rho_i-\rho_i(\mb 0)}}(S)\geq0
\qquad\text{for every nonempty }S\subseteq N(i).
\end{equation}
If~$\lambda_i(\mb 0)=0$, then
\eqref{eq:patch-positivity-first-inequality} and~$\lambda_i\geq0$ imply
$\lambda_i\equiv0$: for a neighbourhood configuration whose set of~$1$'s is
$T\subseteq N(i)$,
\begin{equation*}
0\leq\lambda_i(\eta)
=\sum_{S\subseteq T}\widehat{\lambda_i}(S)
\leq0.
\end{equation*}
Hence~$\lambda_i\rho_i\equiv0$, and
\eqref{eq:patch-positivity-second-inequality} is automatic.

Conversely, suppose that~\eqref{eq:patch-positivity-first-inequality} and
\eqref{eq:patch-positivity-second-inequality} hold. The incoming-initial
contributions are nonnegative by the first part of the proof, the
$\mathsf{OO}$ contribution is nonnegative by the first inequality, and both
terms in~\eqref{eq:outgoing-incoming-numerator} are nonnegative. These are all
four distinct contribution formulas. Since~$i$ was arbitrary, this proves the
coefficient criterion in part~\textup{(i)} of
Theorem~\ref{thm:patch-positivity-order}.

\subsection{Patch threshold}
\label{subsec:critical-density}

To complete part~\textup{(i)} of Theorem~\ref{thm:patch-positivity-order}, it
remains to compute the patch threshold profile. Patch positivity controls
$C(P)$ for the full patch family, but the finite-time representation also
contains end contributions~$C(p,P)$. The threshold~$p_i^\star$ is defined as the smallest
$p\in[0,1]$ such that~$C(p,P)\geq0$ for every end patch based at~$i$. Every end
patch has terminal label~$\mathsf E$, so it has type~$\mathsf{IE}$ or
$\mathsf{OE}$.

Assume the spin system is patch-positive and fix an end patch~$P$ based at~$i$ and write~$\Delta=e(P)-s(P)$. For a
$\mathsf{IE}$ patch,~\eqref{eq:end-contribution-formulas} gives
\begin{equation*}
C(p,P)=\frac{\psi_i(\Delta,p)}{\varphi_i(\Delta)}.
\end{equation*}
For~$p\in[0,1]$ we have~$\psi_i(\Delta,p)\geq0$ and~$\varphi_i(\Delta)>0$. Thus
$\mathsf{IE}$ patches impose no restriction on~$p$, and~$p_i^\star$ is
determined entirely by~$\mathsf{OE}$ patches.

If~$\lambda_i(\mb 0)=0$, the coefficient criterion proved above forces
$\lambda_i\equiv0$. There are then no outgoing interactions based at~$i$, so
$p_i^\star=0$. Suppose therefore that~$\lambda_i(\mb 0)>0$, and let~$P$ be an
$\mathsf{OE}$ end patch whose initial outgoing interaction has nonempty
target~$S$. Since its denominator is positive, the sign of~$C(p,P)$ is the sign
of the numerator,
\begin{equation*}
N_{i,S}(\Delta,p)
=
\widehat{\lambda_i\rho_i}(S)
-
\widehat{\lambda_i}(S)\psi_i(\Delta,p).
\end{equation*}
By Appendix~\ref{app:positivity-algebra},
\begin{equation*}
\psi_i(\Delta,p)
=
\rho_i(\mb 0)
+
\bb{p-\rho_i(\mb 0)}e^{-\lambda_i(\mb 0)\Delta}.
\end{equation*}
Consequently,~$N_{i,S}(\Delta,p)$ is affine in
$e^{-\lambda_i(\mb 0)\Delta}$, with endpoint values
\begin{align*}
N_{i,S}(0,p)
&=
\widehat{\lambda_i\rho_i}(S)
-p\widehat{\lambda_i}(S),
\\
\lim_{\Delta\to\infty}N_{i,S}(\Delta,p)
&=
\widehat{\lambda_i\bb{\rho_i-\rho_i(\mb 0)}}(S).
\end{align*}
The second endpoint is nonnegative by
\eqref{eq:patch-positivity-second-inequality}. It follows that the
$\mathsf{OE}$ contribution is nonnegative for every patch length exactly when
\begin{equation*}
\widehat{\lambda_i\rho_i}(S)
-p\widehat{\lambda_i}(S)
\geq0.
\end{equation*}

If~$\widehat{\lambda_i}(S)=0$, then
\eqref{eq:patch-positivity-second-inequality} gives
$\widehat{\lambda_i\rho_i}(S)\geq0$, so~$S$ imposes no restriction. If instead
$\widehat{\lambda_i}(S)<0$, the preceding inequality is equivalent to
\begin{equation*}
p
\geq
\frac{\widehat{\lambda_i\rho_i}(S)}{\widehat{\lambda_i}(S)}.
\end{equation*}
Since~$N(i)$ is finite, taking the maximum over all nonempty targets gives
\begin{equation*}
p_i^\star
=
\max\bb{
\cb{0}
\cup
\cb{
\frac{\widehat{\lambda_i\rho_i}(S)}{\widehat{\lambda_i}(S)}:
\vn\neq S\subseteq N(i),\ \widehat{\lambda_i}(S)<0
}
}.
\end{equation*}
Finally, for every~$S$ appearing in this maximum,
\eqref{eq:patch-positivity-second-inequality} gives
\begin{equation*}
\widehat{\lambda_i\rho_i}(S)
-\rho_i(\mb 0)\widehat{\lambda_i}(S)
\geq0.
\end{equation*}
Dividing by~$\widehat{\lambda_i}(S)<0$ shows that
\begin{equation*}
\frac{\widehat{\lambda_i\rho_i}(S)}{\widehat{\lambda_i}(S)}
\leq
\rho_i(\mb 0).
\end{equation*}
Hence~$p_i^\star\leq\rho_i(\mb 0)$, which completes the proof of
part~\textup{(i)} of Theorem~\ref{thm:patch-positivity-order}.

\subsection{Interpretation of patch positivity}
\label{subsec:positivity-interpretation}

% ============================================================================
% BLURB A. Dynamic meaning of the coefficient criterion
%
% Reader question:
%   What does patch positivity mean for the local dynamics, rather than for
%   M\"obius coefficients?
%
% Job:
%   Put
%       h_i = \lambda_i(\rho_i-\rho_i(\mb 0)).
%   From the two coefficient inequalities proved in
%   Subsection~\ref{subsec:positivity-criterion}, explain that adding neighbouring
%   1's decreases \lambda_i and increases h_i from 0. Wherever updates remain
%   possible,
%       \rho_i = \rho_i(\mb 0)+h_i/\lambda_i.
%   If \eta\leq\xi on N(i) and \lambda_i(\xi)>0, then
%       h_i(\eta)\leq h_i(\xi),
%       \lambda_i(\eta)\geq\lambda_i(\xi)>0,
%   and hence \rho_i(\eta)\leq\rho_i(\xi).
%
%   State the dynamic interpretation prominently: neighbouring 1's make updates
%   less frequent and, when an update occurs, bias the refreshed spin toward 1.
%   Give the equivalent vacancy formulation: neighbouring 0's facilitate
%   updates and bias refreshes toward 0.
%
% Reader takeaway:
%   The defining dynamic picture of a patch-positive system is that neighbouring
%   1's constrain updates and bias those updates toward 1.
%
% Keep out:
%   The relation with KCSMs; centered moments; the proof of part (ii).
% ============================================================================

% ============================================================================
% BLURB B. Stronger than ordinary monotonicity
%
% Reader question:
%   Is patch positivity merely the pointwise condition that \lambda_i decreases
%   and \rho_i increases when neighbouring 1's are added?
%
% Job:
%   Explain that these monotonicities are consequences, not an equivalent
%   characterization. Patch positivity fixes the signs of all nonconstant
%   M\"obius coefficients. If useful, write
%       d_i = \lambda_i(\mb 0)-\lambda_i
%   and note briefly that d_i has nonnegative M\"obius coefficients (a totally
%   monotone set function). Do not develop this terminology; the main point is
%   that the condition controls all mixed effects of adding neighbouring 1's,
%   not only first-order monotonicity.
%
%   Handle the zero-rate caveat here. Since \lambda_i is nonincreasing, once it
%   reaches zero it remains zero after further 1's are added. On this set the
%   value of \rho_i is dynamically irrelevant because no refresh occurs.
%
% Reader takeaway:
%   The simple dynamic rule is the useful interpretation, but the actual
%   patch-positivity hypothesis is a stronger coefficientwise condition.
% ============================================================================

% ============================================================================
% BLURB C. Relation with kinetically constrained spin models
%
% Reader question:
%   How does this class relate to the KCSMs introduced earlier in the paper?
%
% Job:
%   Recall that in a standard KCSM the refresh law is fixed,
%       \rho_i(\eta)\equiv p,
%   while the neighbourhood affects the update activity \lambda_i. Thus KCSMs
%   are the special case in which the neighbourhood controls whether or how
%   often a site updates, but not the Bernoulli refresh distribution.
%
%   Explain that the present class allows the neighbourhood to affect both the
%   update rate and the refresh bias. Under patch positivity the two effects are
%   aligned: neighbouring 1's suppress updates and favor 1 when an update occurs.
%
%   Locate the class without claiming that it contains all KCSMs. Vacancy-
%   facilitated examples such as FA-1f fit this picture; BABP and the contact
%   process also fit the patch-positive class. Cooperative KCSMs need not be
%   patch-positive, because ordinary monotonicity of a kinetic constraint does
%   not imply the required M\"obius-sign condition.
%
% Reader takeaway:
%   Patch-positive systems are best viewed through the local rule that 1's
%   constrain updates and bias refreshes toward 1; this class intersects the
%   familiar KCSM class but is not identical to it.
%
% Keep out:
%   A catalogue of examples or application-specific calculations; these belong
%   in the applications section.
% ============================================================================
