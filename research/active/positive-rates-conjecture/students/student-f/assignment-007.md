# Student F assignment 007: block contraction of the trail-generated mass/disagreement stack

Work on branch `research/positive-rates-conjecture`.

**Stop Assignment 006 now.** The principal's continued calculation has produced an exact depth-two counterexample to the one-generation contraction `(T)` that Assignment 006 asked you to pursue. Preserve any useful audit work you already did on the trail identity, but do not spend further time trying to prove one-step centered-transfer contraction.

Read immediately:

- `meetings/006-one-step-transfer-refuted-block-stack-target.md`;
- `notes/principal-centered-trail-update2.md`;
- `notes/principal-centered-trail-reduction.md` for the underlying trail formula;
- your Assignments 003--004 for the coupling/coalescence tools;
- Meeting 005 only with the explicit correction that its target `(T)` is now false.

Student G is still finishing Assignment 002. Do not wait for it.

## What is now established / claimed by the principal update

Use

$$
B=b+c-a,\qquad g=b-a,\qquad \omega=1-c+a.
$$

The canonical predecessor-trail decomposition remains the active reduction. The right-region factor has the stronger segmentwise bound

$$
|R_{\gamma,t}(\eta)|\le C_A\prod_{k=1}^n s_1(u_k),
$$

so with

$$
w(u)=e^{-\omega u}s_1(u)
$$

the correct sufficient quantity is

$$
J_{x,r}
=B g^{n-1}
\int_{(0,\infty)^n}
\left(\prod_{k=1}^n w(u_k)\right)
|\pi^0_{m,r}(F_{x,u})|\,du.
\tag{J}
$$

The principal update gives an exact near-East depth-two obstruction:

$$
\frac{g}{|m_\varepsilon|}
\int_0^\infty e^{-\omega u}|A_{2,\varepsilon}(u)|\,du\to\frac32,
$$

and even with right killing

$$
\frac{g}{|m_\varepsilon|}
\int_0^\infty w(u)|A_{2,\varepsilon}(u)|\,du\to\frac75.
$$

Therefore **one-step `(T)` is false**. This is not negotiable unless you find an error in the exact two-site calculation.

The new exact insertion identity is

$$
g\,\mu(h_{p_*}(\eta_y)f)
=(Br-c)\bar\mu(f)
+Br(1-r)(\mu^1-\mu^0)(f),
\tag{MD}
$$

which splits each transfer into a signed mass channel and a positive disagreement channel.

The principal update also gives a negative-drift unresolved disagreement stack. In its reset coupling, if `K` is the number of consecutive levels resolved during one zero-boundary evolution segment,

$$
\mathbb P(K\ge j)
\ge
\frac{B+\omega}{B+2\omega}2^{-(j-1)},
$$

so

$$
\mathbb EK>1,
$$

and for stack height `H`,

$$
H'\le(H-K)_+ +1
$$

has strict negative drift for large `H`.

This is where your live-coupling work becomes directly relevant.

## Phase A: urgent independent audit of the new correction

Before building on it, independently check the pieces that killed Assignment 006:

1. derive the segmentwise survival factor `s_1(u)` and its product over trail segments;
2. verify the resolvent formula for
   $$
   Z=\int_0^\infty e^{-\omega u}s_1(u)\,du;
   $$
3. reproduce the exact near-East formulas for `m_epsilon`, `M_{2,epsilon}`, and `A_{2,epsilon}(u)`;
4. prove the sign change and the limits `3/2` and `7/5`;
5. check the claimed easy region `max{c,g}Z<1` and state exactly what residual subregion it covers;
6. rederive the mass/disagreement identity `(MD)`;
7. audit the stack-height drift argument, preferably relating it explicitly to your universal coalescence-hazard lemma.

If any of these fails, identify the first false statement and stop there. Otherwise treat one-step contraction as definitively closed.

## Phase B: attack the actual remaining lemma

The target is no longer a one-generation norm. Seek a **parameter-dependent block contraction**.

Construct a norm or Lyapunov functional on decompositions into:

- signed mass components;
- coupled disagreement pairs / conditional-law differences;
- a weight for unresolved disagreement-stack height;

such that for some finite `m_0=m_0(a,b,c)` and `theta<1`, the complete right-weighted transfer obeys

$$
\boxed{
\|T^{m_0}\nu\|_*\le\theta\|\nu\|_*.
}
\tag{BC}
$$

The theorem only needs to hold on the class generated by the predecessor-trail transfers from the zero-boundary invariant laws.

A valid proof must explain how `(BC)` implies `J_{x,r}->0` and therefore decay of the nonempty-exit term.

## What you must not do

Do not:

- try to rescue one-step `(T)` by changing the norm without confronting the exact depth-two factor `7/5`;
- replace `mu^1-mu^0` by unrestricted total variation and call that a closure;
- compute depth three, four, etc. as isolated scalar diagnostics;
- return to cellwise regional positivity;
- treat negative stack-height drift alone as enough to control signed mass;
- omit the no-exit term if you reconstruct the final convergence implication.

The core unresolved issue is **branching through `(MD)`**: after each transfer, mass and disagreement components split again. You need a finite weighted bookkeeping scheme in which stack regeneration produces enough mass loss to dominate that branching over a block.

## Promising routes

You have broad freedom. Particularly relevant possibilities:

1. Use a Foster--Lyapunov norm on `(signed mass magnitude, disagreement-stack height)` and prove contraction only after the stack hits a bounded set.
2. Build a finite type kernel for bounded stack heights plus a geometric tail controlled by the negative drift.
3. Use your common-uniform coupling to control `mu^1-mu^0` in a Wasserstein/disagreement norm that composes under zero-boundary evolution.
4. Derive a renewal decomposition at genuine stack regenerations; the mass channel then has multiplier tending to `2/5` near East.
5. Prove an exact obstruction if branching can overpower regeneration in every natural finite-context norm.

Stress-test near East. A contraction constant may tend to one as `epsilon->0`; the theorem only needs `theta<1` at each strict positive-rate parameter point.

## Durable output

Commit to

`research/active/positive-rates-conjecture/students/student-f/007-block-mass-disagreement-contraction.md`

with exact code/certificates beside it if useful.

End with one of:

- `block mass/disagreement contraction proved: ...`;
- `criterion J proved by another all-depth mechanism: ...`;
- `principal update fails at: ...`;
- `block-contraction route fails because: ...`;
- `unresolved after substantive work; exact branching blocker: ...`.
